{
  "name": "snapmind-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix .. run backend-core:build && wrangler dev",
    "deploy": "npm --prefix .. run backend-core:build && wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.79.0",
    "hono": "^4.7.0",
    "mammoth": "^1.12.0",
    "officeparser": "^6.0.4",
    "openai": "^4.77.0",
    "pdf-parse": "^2.4.5",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "wrangler": "^4.22.0"
  }
}
